api:
  dashboard: false

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    exposedByDefault: false
    network: nexus-runtime
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: INFO
