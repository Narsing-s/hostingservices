{
  "name": "nexus-hosting",
  "private": true,
  "version": "0.5.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "dev:api": "npm run dev --workspace apps/api",
    "dev:engine": "npm run dev --workspace apps/engine",
    "dev:agent": "npm run dev --workspace apps/agent",
    "build": "npm run build --workspace apps/web && npm run build --workspace apps/api && npm run build --workspace apps/engine && npm run build --workspace apps/agent && npm run build --workspace @nexus/providers && npm run build --workspace @nexus/secrets && npm run build --workspace @nexus/platform-contracts",
    "start": "npm run start --workspace apps/web",
    "typecheck": "npm run build --workspace apps/api && npm run build --workspace apps/engine && npm run build --workspace apps/agent && npm run build --workspace @nexus/platform-contracts",
    "test:platform": "npm run build --workspace @nexus/platform-contracts",
    "cli:link": "npm link ./packages/cli",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "infra:logs": "docker compose logs -f"
  },
  "engines": { "node": ">=20" }
}
