import pg from 'pg';
import crypto from 'node:crypto';
const { Pool } = pg;
const pool = new Pool({ connectionString: process.env.DATABASE_URL ?? 'postgres://nexus:nexus_dev_only@127.0.0.1:5432/nexus' });

export async function initDb(){await pool.query(`CREATE TABLE IF NOT EXISTS users (id uuid PRIMARY KEY,email text UNIQUE,name text NOT NULL,avatar_url text,password_hash text,created_at timestamptz NOT NULL,updated_at timestamptz NOT NULL);CREATE TABLE IF NOT EXISTS sessions (id uuid PRIMARY KEY,user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,token_hash text UNIQUE NOT NULL,expires_at timestamptz NOT NULL,created_at timestamptz NOT NULL);CREATE TABLE IF NOT EXISTS oauth_accounts (id uuid PRIMARY KEY,user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,provider text NOT NULL,provider_account_id text NOT NULL,access_token text,created_at timestamptz NOT NULL,updated_at timestamptz NOT NULL,UNIQUE(provider,provider_account_id));CREATE INDEX IF NOT EXISTS sessions_token_idx ON sessions(token_hash);CREATE TABLE IF NOT EXISTS projects (id uuid PRIMARY KEY,name text NOT NULL,repo text,created_at timestamptz NOT NULL);CREATE TABLE IF NOT EXISTS deployments (id uuid PRIMARY KEY,project_id uuid NOT NULL REFERENCES projects(id) ON DELETE CASCADE,status text NOT NULL,image text NOT NULL,repo text,runtime jsonb,created_at timestamptz NOT NULL);CREATE TABLE IF NOT EXISTS deployment_events (id uuid PRIMARY KEY,deployment_id uuid NOT NULL REFERENCES deployments(id) ON DELETE CASCADE,phase text NOT NULL,message text NOT NULL,metadata jsonb NOT NULL DEFAULT '{}'::jsonb,created_at timestamptz NOT NULL DEFAULT NOW());CREATE INDEX IF NOT EXISTS deployment_events_deployment_idx ON deployment_events(deployment_id,created_at);CREATE TABLE IF NOT EXISTS recovery_points (id uuid PRIMARY KEY,project_id uuid NOT NULL REFERENCES projects(id) ON DELETE CASCADE,deployment_id uuid REFERENCES deployments(id) ON DELETE SET NULL,service_id text,label text NOT NULL,image text,runtime jsonb NOT NULL DEFAULT '{}'::jsonb,created_at timestamptz NOT NULL DEFAULT NOW());CREATE INDEX IF NOT EXISTS recovery_points_project_idx ON recovery_points(project_id,created_at DESC);CREATE TABLE IF NOT EXISTS application_graph_nodes (id uuid PRIMARY KEY,project_id uuid NOT NULL REFERENCES projects(id) ON DELETE CASCADE,service_id text,node_key text NOT NULL,name text NOT NULL,type text NOT NULL,status text NOT NULL DEFAULT 'unknown',metadata jsonb NOT NULL DEFAULT '{}'::jsonb,created_at timestamptz NOT NULL DEFAULT NOW(),updated_at timestamptz NOT NULL DEFAULT NOW(),UNIQUE(project_id,node_key));CREATE INDEX IF NOT EXISTS application_graph_nodes_project_idx ON application_graph_nodes(project_id);CREATE TABLE IF NOT EXISTS application_graph_edges (id uuid PRIMARY KEY,project_id uuid NOT NULL REFERENCES projects(id) ON DELETE CASCADE,source_node_id uuid NOT NULL REFERENCES application_graph_nodes(id) ON DELETE CASCADE,target_node_id uuid NOT NULL REFERENCES application_graph_nodes(id) ON DELETE CASCADE,type text NOT NULL DEFAULT 'depends_on',metadata jsonb NOT NULL DEFAULT '{}'::jsonb,created_at timestamptz NOT NULL DEFAULT NOW(),UNIQUE(source_node_id,target_node_id,type));CREATE INDEX IF NOT EXISTS application_graph_edges_project_idx ON application_graph_edges(project_id);CREATE TABLE IF NOT EXISTS autoscaling_decisions (id uuid PRIMARY KEY,service_id text NOT NULL,current_replicas integer NOT NULL,desired_replicas integer NOT NULL,reason text NOT NULL,metrics jsonb NOT NULL DEFAULT '{}'::jsonb,created_at timestamptz NOT NULL DEFAULT NOW());CREATE INDEX IF NOT EXISTS autoscaling_decisions_service_idx ON autoscaling_decisions(service_id,created_at DESC);CREATE TABLE IF NOT EXISTS domains (id uuid PRIMARY KEY,project_id uuid NOT NULL REFERENCES projects(id) ON DELETE CASCADE,domain text NOT NULL UNIQUE,verification_token text NOT NULL,status text NOT NULL DEFAULT 'pending',verified_at timestamptz,created_at timestamptz NOT NULL);CREATE TABLE IF NOT EXISTS github_deliveries (delivery_id text PRIMARY KEY,received_at timestamptz NOT NULL DEFAULT NOW());CREATE INDEX IF NOT EXISTS deployments_project_idx ON deployments(project_id,created_at DESC);CREATE INDEX IF NOT EXISTS projects_repo_idx ON projects(repo);CREATE INDEX IF NOT EXISTS domains_project_idx ON domains(project_id);`);}
export async function createUser(user:{id:string;email?:string|null;name:string;avatarUrl?:string|null;passwordHash?:string|null}){const now=new Date().toISOString();await pool.query('INSERT INTO users (id,email,name,avatar_url,password_hash,created_at,updated_at) VALUES ($1,$2,$3,$4,$5,$6,$6)',[user.id,user.email??null,user.name,user.avatarUrl??null,user.passwordHash??null,now]);return getUserById(user.id);}
export async function getUserById(id:string){const {rows}=await pool.query('SELECT id,email,name,avatar_url AS "avatarUrl",created_at AS "createdAt" FROM users WHERE id=$1',[id]);return rows[0];}
export async function getUserByEmail(email:string){const {rows}=await pool.query('SELECT id,email,name,avatar_url AS "avatarUrl",password_hash AS "passwordHash",created_at AS "createdAt" FROM users WHERE lower(email)=lower($1) LIMIT 1',[email]);return rows[0];}
export async function upsertOAuthAccount(input:{provider:string;providerAccountId:string;email?:string|null;name:string;avatarUrl?:string|null;accessToken?:string|null}){const existing=await pool.query('SELECT user_id AS "userId" FROM oauth_accounts WHERE provider=$1 AND provider_account_id=$2',[input.provider,input.providerAccountId]);let userId:string;if(existing.rows[0])userId=existing.rows[0].userId;else{const byEmail=input.email?await getUserByEmail(input.email):undefined;userId=byEmail?.id??crypto.randomUUID();if(!byEmail)await createUser({id:userId,email:input.email,name:input.name,avatarUrl:input.avatarUrl});const now=new Date().toISOString();await pool.query('INSERT INTO oauth_accounts (id,user_id,provider,provider_account_id,access_token,created_at,updated_at) VALUES ($1,$2,$3,$4,$5,$6,$6)',[crypto.randomUUID(),userId,input.provider,input.providerAccountId,input.accessToken??null,now]);return getUserById(userId);}await pool.query('UPDATE oauth_accounts SET access_token=$3,updated_at=NOW() WHERE provider=$1 AND provider_account_id=$2',[input.provider,input.providerAccountId,input.accessToken??null]);await pool.query('UPDATE users SET name=$2,avatar_url=$3,updated_at=NOW() WHERE id=$1',[userId,input.name,input.avatarUrl??null]);return getUserById(userId);}
export async function getOAuthAccessToken(userId:string,provider:string){const {rows}=await pool.query('SELECT access_token AS "accessToken" FROM oauth_accounts WHERE user_id=$1 AND provider=$2 LIMIT 1',[userId,provider]);return rows[0]?.accessToken as string|undefined;}
export async function createSession(userId:string,tokenHash:string,expiresAt:string){const id=crypto.randomUUID();await pool.query('INSERT INTO sessions (id,user_id,token_hash,expires_at,created_at) VALUES ($1,$2,$3,$4,NOW())',[id,userId,tokenHash,expiresAt]);return id;}
export async function getUserBySession(tokenHash:string){const {rows}=await pool.query('SELECT u.id,u.email,u.name,u.avatar_url AS "avatarUrl",u.created_at AS "createdAt" FROM sessions s JOIN users u ON u.id=s.user_id WHERE s.token_hash=$1 AND s.expires_at>NOW() LIMIT 1',[tokenHash]);return rows[0];}
export async function deleteSession(tokenHash:string){await pool.query('DELETE FROM sessions WHERE token_hash=$1',[tokenHash]);}
export async function listProjects(){const {rows}=await pool.query('SELECT id,name,repo,created_at AS "createdAt" FROM projects ORDER BY created_at DESC');return rows;}
export async function createProject(project:{id:string;name:string;repo?:string;createdAt:string}){await pool.query('INSERT INTO projects (id,name,repo,created_at) VALUES ($1,$2,$3,$4)',[project.id,project.name,project.repo??null,project.createdAt]);return project;}
export async function findProjectByRepo(repo:string){const {rows}=await pool.query('SELECT id,name,repo,created_at AS "createdAt" FROM projects WHERE repo=$1 LIMIT 1',[repo]);return rows[0] as {id:string;name:string;repo?:string;createdAt:string}|undefined;}
export async function projectExists(id:string){const result=await pool.query('SELECT 1 FROM projects WHERE id=$1',[id]);return result.rowCount===1;}
export async function listDeployments(){const {rows}=await pool.query('SELECT id,project_id AS "projectId",status,image,repo,runtime,created_at AS "createdAt" FROM deployments ORDER BY created_at DESC');return rows;}
export async function createDeployment(deployment:{id:string;projectId:string;status:string;image:string;repo?:string;createdAt:string}){await pool.query('INSERT INTO deployments (id,project_id,status,image,repo,created_at) VALUES ($1,$2,$3,$4,$5,$6)',[deployment.id,deployment.projectId,deployment.status,deployment.image,deployment.repo??null,deployment.createdAt]);}
export async function updateDeployment(id:string,status:string,runtime?:unknown){await pool.query('UPDATE deployments SET status=$2,runtime=COALESCE($3::jsonb,runtime) WHERE id=$1',[id,status,runtime===undefined?null:JSON.stringify(runtime)]);}
export async function addDeploymentEvent(deploymentId:string,phase:string,message:string,metadata:unknown={}){await pool.query('INSERT INTO deployment_events (id,deployment_id,phase,message,metadata) VALUES ($1,$2,$3,$4,$5::jsonb)',[crypto.randomUUID(),deploymentId,phase,message,JSON.stringify(metadata)]);}
export async function listDeploymentEvents(deploymentId:string){const {rows}=await pool.query('SELECT id,phase,message,metadata,created_at AS "createdAt" FROM deployment_events WHERE deployment_id=$1 ORDER BY created_at ASC',[deploymentId]);return rows;}
export async function createRecoveryPoint(input:{projectId:string;deploymentId?:string;serviceId?:string;label:string;image?:string;runtime?:unknown}){const id=crypto.randomUUID();await pool.query('INSERT INTO recovery_points (id,project_id,deployment_id,service_id,label,image,runtime) VALUES ($1,$2,$3,$4,$5,$6,$7::jsonb)',[id,input.projectId,input.deploymentId??null,input.serviceId??null,input.label,input.image??null,JSON.stringify(input.runtime??{})]);return {id,...input,createdAt:new Date().toISOString()};}
export async function listRecoveryPoints(projectId:string){const {rows}=await pool.query('SELECT id,project_id AS "projectId",deployment_id AS "deploymentId",service_id AS "serviceId",label,image,runtime,created_at AS "createdAt" FROM recovery_points WHERE project_id=$1 ORDER BY created_at DESC',[projectId]);return rows;}
export async function upsertGraphNode(input:{projectId:string;serviceId?:string;nodeKey:string;name:string;type:string;status?:string;metadata?:unknown}){const {rows}=await pool.query('INSERT INTO application_graph_nodes (id,project_id,service_id,node_key,name,type,status,metadata) VALUES ($1,$2,$3,$4,$5,$6,$7,$8::jsonb) ON CONFLICT(project_id,node_key) DO UPDATE SET name=EXCLUDED.name,type=EXCLUDED.type,status=EXCLUDED.status,metadata=EXCLUDED.metadata,updated_at=NOW() RETURNING id,project_id AS "projectId",service_id AS "serviceId",node_key AS "nodeKey",name,type,status,metadata,created_at AS "createdAt",updated_at AS "updatedAt"',[crypto.randomUUID(),input.projectId,input.serviceId??null,input.nodeKey,input.name,input.type,input.status??'unknown',JSON.stringify(input.metadata??{})]);return rows[0];}
export async function upsertGraphEdge(input:{projectId:string;sourceNodeId:string;targetNodeId:string;type?:string;metadata?:unknown}){const {rows}=await pool.query('INSERT INTO application_graph_edges (id,project_id,source_node_id,target_node_id,type,metadata) VALUES ($1,$2,$3,$4,$5,$6::jsonb) ON CONFLICT(source_node_id,target_node_id,type) DO UPDATE SET metadata=EXCLUDED.metadata RETURNING id,project_id AS "projectId",source_node_id AS "sourceNodeId",target_node_id AS "targetNodeId",type,metadata,created_at AS "createdAt"',[crypto.randomUUID(),input.projectId,input.sourceNodeId,input.targetNodeId,input.type??'depends_on',JSON.stringify(input.metadata??{})]);return rows[0];}
export async function getApplicationGraph(projectId:string){const nodes=await pool.query('SELECT id,service_id AS "serviceId",node_key AS "nodeKey",name,type,status,metadata FROM application_graph_nodes WHERE project_id=$1 ORDER BY created_at',[projectId]);const edges=await pool.query('SELECT id,source_node_id AS "sourceNodeId",target_node_id AS "targetNodeId",type,metadata FROM application_graph_edges WHERE project_id=$1 ORDER BY created_at',[projectId]);return {nodes:nodes.rows,edges:edges.rows};}
export async function recordAutoscalingDecision(input:{serviceId:string;currentReplicas:number;desiredReplicas:number;reason:string;metrics?:unknown}){await pool.query('INSERT INTO autoscaling_decisions (id,service_id,current_replicas,desired_replicas,reason,metrics) VALUES ($1,$2,$3,$4,$5,$6::jsonb)',[crypto.randomUUID(),input.serviceId,input.currentReplicas,input.desiredReplicas,input.reason,JSON.stringify(input.metrics??{})]);}
export async function createDomain(domain:{id:string;projectId:string;domain:string;verificationToken:string;createdAt:string}){await pool.query('INSERT INTO domains (id,project_id,domain,verification_token,created_at) VALUES ($1,$2,$3,$4,$5)',[domain.id,domain.projectId,domain.domain,domain.verificationToken,domain.createdAt]);return domain;}
export async function getDomain(id:string){const {rows}=await pool.query('SELECT id,project_id AS "projectId",domain,verification_token AS "verificationToken",status,verified_at AS "verifiedAt",created_at AS "createdAt" FROM domains WHERE id=$1',[id]);return rows[0];}
export async function getVerifiedDomain(projectId:string,domain:string){const {rows}=await pool.query('SELECT id,domain,status FROM domains WHERE project_id=$1 AND domain=$2 AND status=\'verified\' LIMIT 1',[projectId,domain]);return rows[0];}
export async function markDomainVerified(id:string){await pool.query('UPDATE domains SET status=\'verified\',verified_at=NOW() WHERE id=$1',[id]);}
export async function listDomains(projectId?:string){const {rows}=await pool.query(projectId?'SELECT id,project_id AS "projectId",domain,status,verified_at AS "verifiedAt",created_at AS "createdAt" FROM domains WHERE project_id=$1 ORDER BY created_at DESC':'SELECT id,project_id AS "projectId",domain,status,verified_at AS "verifiedAt",created_at AS "createdAt" FROM domains ORDER BY created_at DESC',[...(projectId?[projectId]:[])]);return rows;}
export async function claimGithubDelivery(deliveryId:string){const result=await pool.query('INSERT INTO github_deliveries (delivery_id) VALUES ($1) ON CONFLICT (delivery_id) DO NOTHING',[deliveryId]);return result.rowCount===1;}
